---
- name: Converge
  hosts: all
  gather_facts: false
  tasks:
    - name: "Include acme.ansible_install_docker_compose"
      ansible.builtin.include_role:
        name: "acme.ansible_install_docker_compose"
